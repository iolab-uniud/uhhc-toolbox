## 0.2.1 

### Changed

- Updated homepage and repository URLs in `pyproject.toml` to point to the correct GitHub repository.
- Fixed poetry sections in `pyproject.toml` by moving to the correct `[tool.poetry]` section.
- Updated version to 0.2.1 in `pyproject.toml`.